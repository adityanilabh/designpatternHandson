# tracker progress — personal, and it changes on every click
target-ladder-progress.json
target-ladder-*.json

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# next.js
/.next/
/out/
/build
next-env.d.ts
*.tsbuildinfo

# testing
/coverage
/test-results
/playwright-report

# env — never commit the Supabase service_role key
.env*
!.env.example

# vercel
.vercel

# editor / OS noise
.vscode/
.idea/
Thumbs.db
desktop.ini
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# resolver run log (the cache beside it is kept: it is the provenance record)
scripts/.gen.log
